# Bank-Retail-Transaction-Analysis
Transformed a corrupted 850-row retail banking dataset with structural errors, outliers, and missing timestamps into a flawless, automated Excel reporting system.  : Executed deduplication, data-type forcing, and text normalization (nested IF/TRIM).
